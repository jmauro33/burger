<h1>Burger</h1>

<h2>Eat one for lunch</h2>

<ul>
  {{#each burger}}
    {{#unless try one today}}
      {{> burgers/burger-block Eat=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers are so good</h2>

<ul>
  {{#each burgers}}
    {{#if Eat}}
      {{> burgers/burger-block Eat=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Eat-Da-Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="Hungry">Hungry</label><br>
    <input type="radio" name="hungry" value="1" checked> Burger!<br>
    <input type="radio" name="Full" value="0"> Eaten
  </div>

  <button type="submit">Eat Burger</button>
</form>
